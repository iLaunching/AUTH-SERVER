# Authentication API

Minimal FastAPI service focused on user authentication.

## Deploy to Railway

1. Create new Railway service
2. Connect to GitHub repo: `iLaunching/Ilaunching-SERVERS`
3. Set Root Directory: `auth-api`
4. Deploy

## Endpoints

- `GET /` - Service info
- `GET /health` - Health check
- `GET /docs` - API documentation
